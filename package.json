{
  "name": "node-addon-beginner",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^2.6.2",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.6.3"
  },
  "scripts": {
    "install": "cmake-js compile",
    "build": "tsc -p ./src",
    "start": "tsc -p ./src & node ./dist/main.js",
    "build:dev": "ts-node-dev --respawn -- src/main.ts"
  },
  "dependencies": {
    "bindings": "^1.5.0",
    "node-addon-api": "^4.3.0"
  }
}
